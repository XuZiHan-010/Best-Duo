# M2 前端实施计划 — 选关 + 讨论 + 钟盘骨架

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 LevelSelect / Discussion 两个视图及其所有依赖组件，建立可复用的 ClockBoard SVG 骨架。

**Architecture:** 逐层由内到外构建：纯函数 `conditionText.ts` → 无状态展示组件 → 有状态视图。ClockBoard 在 M2 只渲染空钟盘（无牌），M3 通过传入 `placements` 增量添牌，接口不变。

**Tech Stack:** React + TypeScript + Vite；Socket.IO（已有 `adapter.ts`）；CSS Variables（已有 `tokens.css`）；`@take-time/shared` 类型（单一来源）。

---

## 文件清单

| 操作 | 路径 |
|------|------|
| 新建 | `client/src/lib/conditionText.ts` |
| 修改 | `client/src/styles/global.css`（追加所有新 CSS 节） |
| 新建 | `client/src/components/ConditionList.tsx` |
| 新建 | `client/src/components/ClockBoard.tsx` |
| 新建 | `client/src/components/CountdownTimer.tsx` |
| 新建 | `client/src/components/Chat.tsx` |
| 新建 | `client/src/components/LevelRulesIntro.tsx` |
| 新建 | `client/src/components/RulesPanel.tsx` |
| 修改 | `client/src/views/LevelSelect.tsx`（替换 stub） |
| 修改 | `client/src/views/Discussion.tsx`（替换 stub） |

---

## Task 1：`lib/conditionText.ts` — 条件 → 中文映射

**Files:**
- 新建: `client/src/lib/conditionText.ts`

- [ ] **Step 1：写文件**

```ts
// client/src/lib/conditionText.ts
import type { Condition } from "@take-time/shared";

export function conditionToText(c: Condition): string {
  switch (c.type) {
    case "all-nonempty":
      return "所有区段至少 1 张牌";
    case "min-cards":
      return `区${c.segment} 至少 ${c.count} 张`;
    case "max-cards":
      return `区${c.segment} 最多 ${c.count} 张`;
    case "exact-cards":
      return `区${c.segment} 恰好 ${c.count} 张`;
    case "sum-equals":
      return `区${c.segment} 总和恰好 ${c.value}`;
    case "sum-range":
      return `区${c.segment} 总和 ${c.min}–${c.max}`;
    case "parity":
      return `区${c.segment} 总和为${c.parity === "odd" ? "奇" : "偶"}数`;
    case "non-decreasing":
      return `区${c.segments.join(" ≤ 区")} 依次非递减`;
    case "non-increasing":
      return `区${c.segments.join(" ≥ 区")} 依次非递增`;
    case "adjacent-diff":
      return `区${c.a} 与区${c.b} 总和差 ≤ ${c.maxDiff}`;
    case "placement-order":
      return `第 ${c.order} 张打出的牌必须落在区${c.segment}`;
    case "segment-colors":
      return `区${c.segment} 恰好 ${c.black} 黑 + ${c.white} 白`;
    case "all-distinct":
      return `区${c.segment} 内各牌数值互不相同`;
    case "max-sum-each":
      return `每区段总和 ≤ ${c.value}`;
    default:
      return JSON.stringify(c);
  }
}
```

- [ ] **Step 2：验证 TypeScript 编译通过**

在 `client/` 目录下运行：
```bash
npx tsc --noEmit
```
预期：无报错（若有，修复后继续）。

- [ ] **Step 3：提交**

```bash
git add client/src/lib/conditionText.ts
git commit -m "feat(m2): add conditionText mapping (condition → Chinese)"
```

---

## Task 2：CSS 追加 — 所有新组件样式

**Files:**
- 修改: `client/src/styles/global.css`（在文件末尾追加）

- [ ] **Step 1：在 global.css 末尾追加以下内容**

```css
/* ── sr-only（无障碍隐藏）── */
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0,0,0,0); white-space: nowrap; border-width: 0;
}

/* ── ConditionList ── */
.condition-list { list-style: none; display: flex; flex-direction: column; gap: 6px; }
.condition-list--compact { gap: 3px; }
.condition-list__item {
  display: flex; align-items: baseline; gap: 6px;
  font-size: 14px; line-height: 1.4;
}
.condition-list--compact .condition-list__item { font-size: 12px; color: var(--ink-mut); }
.condition-list__icon { flex-shrink: 0; width: 16px; font-weight: 700; }
.condition-list__item--pass .condition-list__icon { color: var(--success); }
.condition-list__item--fail  .condition-list__icon { color: var(--danger); }
.condition-list__item--fail  .condition-list__text  { color: var(--danger); }
.condition-list__text { color: var(--ink); min-width: 0; word-break: break-word; }
.condition-list__msg {
  margin-left: auto; font-size: 12px;
  font-family: var(--font-mono); color: var(--ink-mut);
  flex-shrink: 0;
}
.condition-list__item--fail .condition-list__msg { color: var(--danger); }

/* ── ClockBoard ── */
.clock-board {
  display: flex; align-items: center; justify-content: center;
  width: 100%; aspect-ratio: 1;
  max-width: 360px; margin: 0 auto;
}
/* M3 交互态（出牌阶段）*/
.clock-board__sector--interactive {
  cursor: pointer;
  transition: opacity var(--dur-fast) var(--ease);
}
.clock-board__sector--interactive:hover { opacity: 0.18 !important; }

/* ── CountdownTimer ── */
.countdown-timer {
  font-family: var(--font-mono);
  font-variant-numeric: tabular-nums;
}
.countdown-timer--warn   { color: var(--warn);   animation: pulse 1s ease infinite; }
.countdown-timer--danger { color: var(--danger); animation: pulse 0.5s ease infinite; }

/* ── Chat ── */
.chat { display: flex; flex-direction: column; height: 100%; min-height: 0; }
.chat__list {
  flex: 1; overflow-y: auto; list-style: none;
  display: flex; flex-direction: column; gap: 5px;
  padding: 12px; min-height: 0;
}
.chat__empty { color: var(--ink-dim); font-size: 13px; font-style: italic; text-align: center; padding: 20px 0; }
.chat__msg {
  display: flex; gap: 4px; font-size: 13px; line-height: 1.5;
  flex-wrap: wrap; min-width: 0; word-break: break-word;
}
.chat__nick { font-weight: 700; color: var(--ink-mut); flex-shrink: 0; }
.chat__msg--mine .chat__nick { color: var(--brass); }
.chat__text { color: var(--ink); }
.chat__form {
  display: flex; gap: 8px;
  padding: 10px 12px;
  border-top: 1px solid var(--hairline);
  flex-shrink: 0;
}
.chat__input {
  flex: 1; padding: 8px 12px;
  background: var(--bg-0); border: 1.5px solid var(--hairline);
  border-radius: 8px; color: var(--ink); font-size: 14px;
  min-width: 0;
  transition: border-color var(--dur-fast) var(--ease);
}
.chat__input:focus { border-color: var(--brass); outline: none; }
.chat__input::placeholder { color: var(--ink-dim); }
.chat__send { flex-shrink: 0; padding: 8px 14px; font-size: 13px; }

/* ── LevelRulesIntro（模态弹窗）── */
.level-rules-intro {
  position: fixed; inset: 0;
  background: rgba(6,10,18,.82);
  backdrop-filter: blur(4px);
  display: flex; align-items: center; justify-content: center;
  padding: 24px; z-index: 100;
}
.level-rules-intro__card {
  width: 100%; max-width: 520px;
  background: var(--bg-2); border: 1px solid var(--hairline);
  border-radius: var(--r-card); padding: 32px 36px;
  box-shadow: var(--shadow-lift);
  display: flex; flex-direction: column; gap: 20px;
  max-height: 90vh; overflow-y: auto;
}
.level-rules-intro__title {
  font-family: var(--font-display); font-size: 20px;
  font-weight: 700; color: var(--brass);
}
.level-rules-intro__divider { border: none; border-top: 1px solid var(--hairline); margin: 0; }
.level-rules-intro__legend-title { font-size: 13px; font-weight: 700; color: var(--ink-mut); margin-bottom: 10px; }
.level-rules-intro__legend-items { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 8px; }
.level-rules-intro__deck-note { font-size: 12px; color: var(--ink-dim); }
.level-rules-intro__btn { width: 100%; justify-content: center; }

/* 卡牌图例小块 */
.card-demo {
  display: inline-flex; align-items: center; justify-content: center;
  width: 44px; height: 64px; border-radius: 8px;
  font-size: 14px; font-weight: 700;
}
.card-demo--white { background: linear-gradient(160deg,#ECE6D8,#C8C0B0); color: #1a1a1a; border: 1px solid #B0A898; }
.card-demo--black { background: linear-gradient(160deg,#1a1a2e,#0d0d1a); color: #ECE6D8; border: 1px solid #3a3a5c; }
.card-demo--blind { background: var(--bg-3); color: var(--ink-dim); border: 1.5px solid var(--brass); font-size: 20px; }
.card-demo--hint  { background: var(--bg-3); color: var(--amber);  border: 1.5px solid var(--amber); box-shadow: 0 0 8px var(--brass-glow); font-size: 20px; }

/* ── RulesPanel ── */
.rules-panel {
  background: var(--bg-2); border: 1px solid var(--hairline);
  border-radius: var(--r-card); padding: 14px 16px;
  display: flex; flex-direction: column; gap: 8px;
}
.rules-panel__title { font-size: 12px; font-weight: 700; color: var(--ink-dim); text-transform: uppercase; letter-spacing: .05em; }

/* ── LevelSelect ── */
.level-select {
  flex: 1; display: flex; flex-direction: column; gap: 16px;
  padding: 24px 32px; max-width: 960px; margin: 0 auto; width: 100%;
}
.level-select__header { display: flex; align-items: center; gap: 16px; flex-wrap: wrap; }
.level-select__progress { font-size: 14px; color: var(--ink-mut); }
.level-select__legend { display: flex; gap: 16px; margin-left: auto; font-size: 12px; color: var(--ink-dim); }
.level-select__legend-item--cleared { color: var(--success); }
.level-select__waiting { font-size: 13px; color: var(--ink-dim); text-align: center; padding: 2px 0; }
.level-select__grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(68px, 1fr));
  gap: 10px;
}
.level-select__card {
  position: relative; aspect-ratio: 1;
  background: var(--bg-2); border: 1.5px solid var(--hairline);
  border-radius: 10px;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: 2px;
  transition: border-color var(--dur-fast) var(--ease),
              background   var(--dur-fast) var(--ease);
}
.level-select__card:not(:disabled):hover { border-color: var(--brass); background: var(--bg-3); }
.level-select__card--cleared {
  border-color: rgba(127,176,105,.5);
  background: rgba(127,176,105,.06);
}
.level-select__card--readonly:not(:disabled) { cursor: default; }
.level-select__num { font-family: var(--font-mono); font-size: 15px; font-weight: 700; color: var(--ink); }
.level-select__card--cleared .level-select__num { color: var(--success); }
.level-select__cleared-badge {
  position: absolute; top: 3px; right: 5px;
  font-size: 9px; color: var(--success); font-weight: 700;
}

/* ── Discussion ── */
.discussion { flex: 1; display: flex; flex-direction: column; min-height: 0; }
.discussion__main { flex: 1; display: flex; min-height: 0; }
.discussion__clock-col {
  flex: 3; display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  gap: 16px; padding: 24px;
  border-right: 1px solid var(--hairline);
}
.discussion__chat-col { flex: 2; display: flex; flex-direction: column; min-height: 0; }
.discussion__begin-btn { width: auto; }
.discussion__conditions {
  border-top: 1px solid var(--hairline);
  padding: 10px 24px;
  flex-shrink: 0;
}
/* 底部条件栏横向排列 */
.discussion__conditions .condition-list {
  flex-direction: row; flex-wrap: wrap; gap: 6px 20px;
}
.discussion__conditions .condition-list__item { font-size: 13px; }
```

- [ ] **Step 2：提交**

```bash
git add client/src/styles/global.css
git commit -m "feat(m2): add CSS for all M2 components"
```

---

## Task 3：`components/ConditionList.tsx`

**Files:**
- 新建: `client/src/components/ConditionList.tsx`

- [ ] **Step 1：写组件**

```tsx
// client/src/components/ConditionList.tsx
import React from "react";
import type { Condition, ConditionResult } from "@take-time/shared";
import { conditionToText } from "../lib/conditionText.js";

interface ConditionListProps {
  conditions: Condition[];
  results?: ConditionResult[];  // Reveal 阶段传入，显示 ✓/✗
  compact?: boolean;            // RulesPanel 精简模式（字号更小）
}

export function ConditionList({ conditions, results, compact }: ConditionListProps) {
  return (
    <ul
      className={`condition-list${compact ? " condition-list--compact" : ""}`}
      role="list"
    >
      {conditions.map((c, i) => {
        const result = results?.[i];
        const cls = result
          ? result.pass
            ? " condition-list__item--pass"
            : " condition-list__item--fail"
          : "";
        return (
          <li key={i} className={`condition-list__item${cls}`}>
            {result && (
              <span className="condition-list__icon" aria-hidden="true">
                {result.pass ? "✓" : "✗"}
              </span>
            )}
            <span className="condition-list__text">{conditionToText(c)}</span>
            {result && (
              <span className="condition-list__msg">{result.message}</span>
            )}
          </li>
        );
      })}
    </ul>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```
预期：无报错。

- [ ] **Step 3：提交**

```bash
git add client/src/components/ConditionList.tsx
git commit -m "feat(m2): add ConditionList component"
```

---

## Task 4：`components/ClockBoard.tsx`

**Files:**
- 新建: `client/src/components/ClockBoard.tsx`

**设计参数（固定于 SVG viewBox 0 0 240 240，中心 120,120）：**
- `R_OUTER = 85` 钟盘外圈半径
- `R_INNER = 28` 中心圆半径
- `R_TICK  = 93` 刻度线外端
- `R_LABEL = 100` 区段标签（S1–S6）半径
- 每段 60°，S1 从 12 点顺时针起

- [ ] **Step 1：写组件**

```tsx
// client/src/components/ClockBoard.tsx
import React from "react";
import type { PublicPlacedCard } from "@take-time/shared";

interface ClockBoardProps {
  centerCap: number | null;
  placements: PublicPlacedCard[][];   // M2 传 [] ，M3 填充
  interactive?: boolean;              // M3 出牌阶段开启
  onSegmentClick?: (segment: number) => void; // M3 用
}

const CX = 120, CY = 120;
const R_OUTER = 85;
const R_INNER = 28;
const R_TICK  = 93;
const R_LABEL = 100;

function sinDeg(d: number) { return Math.sin(d * Math.PI / 180); }
function cosDeg(d: number) { return Math.cos(d * Math.PI / 180); }
// 从 12 点顺时针 angleDeg 度，距圆心 r 处的坐标
function px(r: number, a: number) { return CX + r * sinDeg(a); }
function py(r: number, a: number) { return CY - r * cosDeg(a); }

// 以圆心为顶点的扇形 path（每段 60°）
function sectorPath(startDeg: number, r: number): string {
  const end = startDeg + 60;
  return [
    `M ${CX} ${CY}`,
    `L ${px(r, startDeg).toFixed(2)} ${py(r, startDeg).toFixed(2)}`,
    `A ${r} ${r} 0 0 1 ${px(r, end).toFixed(2)} ${py(r, end).toFixed(2)}`,
    "Z",
  ].join(" ");
}

const SEG_LABELS = ["S1", "S2", "S3", "S4", "S5", "S6"];

export function ClockBoard({
  centerCap,
  placements,
  interactive,
  onSegmentClick,
}: ClockBoardProps) {
  return (
    <div className="clock-board">
      <svg
        viewBox="0 0 240 240"
        width="100%"
        height="100%"
        aria-label="时钟盘"
      >
        {/* 底层背景圆 */}
        <circle cx={CX} cy={CY} r={R_OUTER} fill="#0c1320" stroke="#2a3550" strokeWidth="1" />

        {/* 6 个扇形底色（交替深浅，辅助区分） */}
        {Array.from({ length: 6 }).map((_, i) => (
          <path
            key={i}
            d={sectorPath(i * 60, R_OUTER)}
            fill="#C9A24B"
            opacity={i % 2 === 0 ? 0.07 : 0.04}
            className={interactive ? "clock-board__sector--interactive" : undefined}
            onClick={interactive && onSegmentClick ? () => onSegmentClick(i + 1) : undefined}
            role={interactive ? "button" : undefined}
            aria-label={interactive ? `区段 ${i + 1}` : undefined}
            style={{ cursor: interactive ? "pointer" : undefined }}
          />
        ))}

        {/* 外刻度环 */}
        <circle cx={CX} cy={CY} r={R_OUTER} fill="none" stroke="#C9A24B" strokeWidth="1.2" opacity="0.3" />

        {/* 12 刻度线（每 30°） */}
        {Array.from({ length: 12 }).map((_, i) => (
          <line
            key={i}
            x1={px(R_OUTER, i * 30).toFixed(2)} y1={py(R_OUTER, i * 30).toFixed(2)}
            x2={px(R_TICK,  i * 30).toFixed(2)} y2={py(R_TICK,  i * 30).toFixed(2)}
            stroke="#C9A24B" strokeWidth="1" opacity="0.45"
          />
        ))}

        {/* 6 段分割线（从中心圆边缘到外圈，每 60°） */}
        {Array.from({ length: 6 }).map((_, i) => (
          <line
            key={i}
            x1={px(R_INNER, i * 60).toFixed(2)} y1={py(R_INNER, i * 60).toFixed(2)}
            x2={px(R_OUTER, i * 60).toFixed(2)} y2={py(R_OUTER, i * 60).toFixed(2)}
            stroke="#C9A24B" strokeWidth="1" opacity="0.4"
          />
        ))}

        {/* S1–S6 标签（外圈，每段弧中点） */}
        {SEG_LABELS.map((label, i) => {
          const midDeg = i * 60 + 30; // 每段 60° 的中点
          return (
            <text
              key={i}
              x={px(R_LABEL, midDeg).toFixed(2)}
              y={py(R_LABEL, midDeg).toFixed(2)}
              textAnchor="middle"
              dominantBaseline="central"
              fontSize="10"
              fontWeight={i === 0 ? "700" : "600"}
              fill={i === 0 ? "#C9A24B" : "#9AA4B8"}
              fontFamily="monospace"
            >
              {label}
            </text>
          );
        })}

        {/* 中心圆 */}
        <circle cx={CX} cy={CY} r={R_INNER} fill="#121b2c" stroke="#C9A24B" strokeWidth="1.5" opacity="0.7" />

        {/* centerCap：数字或 ∞ */}
        <text
          x={CX} y={CY}
          textAnchor="middle"
          dominantBaseline="central"
          fontSize="16"
          fontWeight="700"
          fill="#C9A24B"
          fontFamily="monospace"
        >
          {centerCap === null ? "∞" : String(centerCap)}
        </text>

        {/* M3：placements 牌堆渲染占位（目前 placements 忽略） */}
      </svg>
    </div>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```
预期：无报错。

- [ ] **Step 3：提交**

```bash
git add client/src/components/ClockBoard.tsx
git commit -m "feat(m2): add ClockBoard SVG skeleton (6 segments, outer labels, centerCap)"
```

---

## Task 5：`components/CountdownTimer.tsx`

**Files:**
- 新建: `client/src/components/CountdownTimer.tsx`

- [ ] **Step 1：写组件**

```tsx
// client/src/components/CountdownTimer.tsx
import React, { useEffect, useState } from "react";
import { formatMmSs } from "../lib/timeFmt.js";

interface CountdownTimerProps {
  deadline: number;            // Date.now() 毫秒时间戳
  warnThresholdMs?: number;    // 默认 30000（最后 30s 转警告色）
  dangerThresholdMs?: number;  // 默认 2000（最后 2s 闪烁）
}

export function CountdownTimer({
  deadline,
  warnThresholdMs = 30_000,
  dangerThresholdMs = 2_000,
}: CountdownTimerProps) {
  const [remaining, setRemaining] = useState(() =>
    Math.max(0, deadline - Date.now())
  );

  useEffect(() => {
    const tick = () => setRemaining(Math.max(0, deadline - Date.now()));
    const id = setInterval(tick, 500);
    tick();
    return () => clearInterval(id);
  }, [deadline]);

  const isWarn   = remaining <= warnThresholdMs;
  const isDanger = remaining <= dangerThresholdMs;

  return (
    <span
      className={[
        "countdown-timer",
        isWarn   ? "countdown-timer--warn"   : "",
        isDanger ? "countdown-timer--danger" : "",
      ]
        .filter(Boolean)
        .join(" ")}
      aria-live="polite"
      aria-label={`剩余时间 ${formatMmSs(remaining)}`}
    >
      {formatMmSs(remaining)}
    </span>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3：提交**

```bash
git add client/src/components/CountdownTimer.tsx
git commit -m "feat(m2): add CountdownTimer component"
```

---

## Task 6：`components/Chat.tsx`

**Files:**
- 新建: `client/src/components/Chat.tsx`

- [ ] **Step 1：写组件**

```tsx
// client/src/components/Chat.tsx
import React, { useRef, useEffect, useState } from "react";
import type { ChatMessage } from "@take-time/shared";
import { adapter } from "../socket/adapter.js";

interface ChatProps {
  messages: ChatMessage[];
  mySeatId: string | null;
}

export function Chat({ messages, mySeatId }: ChatProps) {
  const [text, setText] = useState("");
  const listRef = useRef<HTMLOListElement>(null);

  // 新消息到来时自动滚到底部
  useEffect(() => {
    const el = listRef.current;
    if (el) el.scrollTop = el.scrollHeight;
  }, [messages]);

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    const trimmed = text.trim();
    if (!trimmed) return;
    adapter.sendChat({ text: trimmed });
    setText("");
  }

  return (
    <div className="chat">
      <ol
        className="chat__list"
        ref={listRef}
        aria-live="polite"
        aria-label="聊天记录"
      >
        {messages.length === 0 && (
          <li className="chat__empty">还没有消息，开始讨论…</li>
        )}
        {messages.map((msg) => (
          <li
            key={msg.id}
            className={`chat__msg${msg.senderSeatId === mySeatId ? " chat__msg--mine" : ""}`}
          >
            <span className="chat__nick">{msg.nick}：</span>
            <span className="chat__text">{msg.text}</span>
          </li>
        ))}
      </ol>

      <form className="chat__form" onSubmit={handleSubmit}>
        <label htmlFor="chat-input" className="sr-only">
          发送消息
        </label>
        <input
          id="chat-input"
          className="chat__input"
          type="text"
          value={text}
          onChange={(e) => setText(e.target.value)}
          placeholder="输入消息…"
          maxLength={200}
          autoComplete="off"
        />
        <button
          type="submit"
          className="btn btn--ghost chat__send"
          disabled={!text.trim()}
        >
          发送
        </button>
      </form>
    </div>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3：提交**

```bash
git add client/src/components/Chat.tsx
git commit -m "feat(m2): add Chat component"
```

---

## Task 7：`components/LevelRulesIntro.tsx`

**Files:**
- 新建: `client/src/components/LevelRulesIntro.tsx`

- [ ] **Step 1：写组件**

```tsx
// client/src/components/LevelRulesIntro.tsx
import React, { useEffect, useRef } from "react";
import type { Condition } from "@take-time/shared";
import { ConditionList } from "./ConditionList.js";

interface LevelRulesIntroProps {
  levelName: string;
  difficulty: string;
  conditions: Condition[];
  onAccept: () => void;
}

export function LevelRulesIntro({
  levelName,
  difficulty,
  conditions,
  onAccept,
}: LevelRulesIntroProps) {
  const btnRef = useRef<HTMLButtonElement>(null);

  // 弹窗出现时聚焦确认按钮
  useEffect(() => {
    btnRef.current?.focus();
  }, []);

  return (
    <div
      className="level-rules-intro"
      role="dialog"
      aria-modal="true"
      aria-labelledby="lri-title"
    >
      <div className="level-rules-intro__card">
        <h2 id="lri-title" className="level-rules-intro__title">
          本关规则 · {levelName} {difficulty}
        </h2>

        <section aria-label="胜利条件">
          <ConditionList conditions={conditions} />
        </section>

        <hr className="level-rules-intro__divider" />

        <section aria-label="卡牌图例">
          <h3 className="level-rules-intro__legend-title">卡牌图例</h3>
          <div className="level-rules-intro__legend-items">
            <span className="card-demo card-demo--white" title="白牌">白</span>
            <span className="card-demo card-demo--black" title="黑牌">黑</span>
            <span className="card-demo card-demo--blind" title="手牌盲位">?</span>
            <span className="card-demo card-demo--hint" title="提示翻开牌">◆</span>
          </div>
          <p className="level-rules-intro__deck-note">
            牌库 24 张：白 1–12 + 黑 1–12，每局发 12 张
          </p>
        </section>

        <button
          ref={btnRef}
          className="btn btn--primary level-rules-intro__btn"
          onClick={onAccept}
        >
          已了解，开始讨论 →
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3：提交**

```bash
git add client/src/components/LevelRulesIntro.tsx
git commit -m "feat(m2): add LevelRulesIntro modal"
```

---

## Task 8：`components/RulesPanel.tsx`

**Files:**
- 新建: `client/src/components/RulesPanel.tsx`

- [ ] **Step 1：写组件**

```tsx
// client/src/components/RulesPanel.tsx
import React from "react";
import type { Condition } from "@take-time/shared";
import { ConditionList } from "./ConditionList.js";

interface RulesPanelProps {
  conditions: Condition[];
}

export function RulesPanel({ conditions }: RulesPanelProps) {
  return (
    <aside className="rules-panel" aria-label="本关规则">
      <h2 className="rules-panel__title">本关规则</h2>
      <ConditionList conditions={conditions} compact />
    </aside>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3：提交**

```bash
git add client/src/components/RulesPanel.tsx
git commit -m "feat(m2): add RulesPanel component"
```

---

## Task 9：`views/LevelSelect.tsx`

**Files:**
- 修改: `client/src/views/LevelSelect.tsx`（完全替换 stub）

**说明：** `PublicRoomState` 不含全部关卡列表，只含 `progress.clearedLevels`（已通关索引数组）。关卡总数用常量 `TOTAL_LEVELS`（当前 10，随关卡设计增加而更新）。房主点击某关卡序号后发 `host:selectLevel { levelIndex }`，后端加载关卡并推送 `room:state`（`phase → discussion`）。

- [ ] **Step 1：替换文件**

```tsx
// client/src/views/LevelSelect.tsx
import React from "react";
import { useRoomStore } from "../store/useRoomStore.js";
import { isHostSelector } from "../store/selectors.js";
import { adapter } from "../socket/adapter.js";

const TOTAL_LEVELS = 10; // 随 levels/ 文件夹关卡增加而更新

export function LevelSelect() {
  const roomState = useRoomStore((s) => s.roomState);
  const isHost    = useRoomStore(isHostSelector);

  if (!roomState) return null;

  const cleared = roomState.progress.clearedLevels;

  return (
    <div className="level-select">
      <div className="level-select__header">
        <span className="level-select__progress">
          已通关 {cleared.length} / {TOTAL_LEVELS}
        </span>
        <span className="level-select__legend">
          <span className="level-select__legend-item level-select__legend-item--cleared">
            ✓ 已通关
          </span>
        </span>
      </div>

      {!isHost && (
        <p className="level-select__waiting" aria-live="polite">
          等待房主选关…
        </p>
      )}

      <div className="level-select__grid" role="list" aria-label="关卡列表">
        {Array.from({ length: TOTAL_LEVELS }).map((_, i) => {
          const levelNum = i + 1;
          const isCleared = cleared.includes(i);
          return (
            <button
              key={i}
              role="listitem"
              className={[
                "level-select__card",
                isCleared ? "level-select__card--cleared" : "",
                !isHost   ? "level-select__card--readonly" : "",
              ]
                .filter(Boolean)
                .join(" ")}
              onClick={isHost ? () => adapter.selectLevel({ levelIndex: i }) : undefined}
              disabled={!isHost}
              aria-label={`第 ${levelNum} 关${isCleared ? "（已通关）" : ""}`}
            >
              <span className="level-select__num">{levelNum}</span>
              {isCleared && (
                <span className="level-select__cleared-badge" aria-hidden="true">
                  ✓
                </span>
              )}
            </button>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3：提交**

```bash
git add client/src/views/LevelSelect.tsx
git commit -m "feat(m2): implement LevelSelect view"
```

---

## Task 10：`views/Discussion.tsx`

**Files:**
- 修改: `client/src/views/Discussion.tsx`（完全替换 stub）

**说明：**
- `rulesAccepted` 为本地 `useState(false)`；每次进入 discussion phase 都会重置（React 重新挂载）。
- 讨论倒计时已由 `TopBar` 通过 `timer:sync` 驱动，Discussion 自身不再单独显示。
- `currentChallenge` 若为 null（极少数竞态情况）则不渲染，等待服务端推送。

- [ ] **Step 1：替换文件**

```tsx
// client/src/views/Discussion.tsx
import React, { useState } from "react";
import { useRoomStore } from "../store/useRoomStore.js";
import { isHostSelector, mySeatIdSelector } from "../store/selectors.js";
import { adapter } from "../socket/adapter.js";
import { LevelRulesIntro } from "../components/LevelRulesIntro.js";
import { ClockBoard } from "../components/ClockBoard.js";
import { ConditionList } from "../components/ConditionList.js";
import { Chat } from "../components/Chat.js";

export function Discussion() {
  const roomState = useRoomStore((s) => s.roomState);
  const isHost    = useRoomStore(isHostSelector);
  const mySeatId  = useRoomStore(mySeatIdSelector);
  const [rulesAccepted, setRulesAccepted] = useState(false);

  if (!roomState?.currentChallenge) {
    return <div className="view-stub">加载关卡中…</div>;
  }

  const { currentChallenge, chat, placements } = roomState;

  if (!rulesAccepted) {
    return (
      <LevelRulesIntro
        levelName={currentChallenge.name}
        difficulty={currentChallenge.difficulty}
        conditions={currentChallenge.conditions}
        onAccept={() => setRulesAccepted(true)}
      />
    );
  }

  return (
    <div className="discussion">
      <div className="discussion__main">
        {/* 左栏：钟面预览 + 房主提前开始按钮 */}
        <div className="discussion__clock-col">
          <ClockBoard
            centerCap={currentChallenge.centerCap}
            placements={placements}
          />
          {isHost && (
            <button
              className="btn btn--ghost discussion__begin-btn"
              onClick={() => adapter.beginPlacement()}
            >
              ▶ 提前开始出牌
            </button>
          )}
          {!isHost && (
            <p className="view-stub" style={{ fontSize: 13 }}>
              等待房主或倒计时结束…
            </p>
          )}
        </div>

        {/* 右栏：聊天 */}
        <div className="discussion__chat-col">
          <Chat messages={chat} mySeatId={mySeatId} />
        </div>
      </div>

      {/* 底部：本关条件清单（横向） */}
      <div className="discussion__conditions" aria-label="本关目标">
        <ConditionList conditions={currentChallenge.conditions} />
      </div>
    </div>
  );
}
```

- [ ] **Step 2：验证 TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3：提交**

```bash
git add client/src/views/Discussion.tsx
git commit -m "feat(m2): implement Discussion view"
```

---

## Task 11：浏览器联调验证

**前提：** 本地已启动后端（`npm run dev` 于项目根目录）和前端（`npm run dev` 于 `client/`）。

- [ ] **Step 1：两个浏览器标签页分别登录（昵称 A、B），验证大厅正常**

- [ ] **Step 2：A 准备 → B 准备 → A（房主）点"开始游戏"→ 进入 LevelSelect**

检查：
- 关卡网格显示 1–10
- A（房主）可点击，B 显示"等待房主选关…"
- 已通关关卡（若有）显示绿色 ✓

- [ ] **Step 3：A 点击第 1 关 → 两端都进入 Discussion**

检查：
- LevelRulesIntro 弹窗出现，显示第 1 关的条件清单
- 卡牌图例（白/黑/盲牌/?/提示牌）正确显示
- 点"已了解，开始讨论 →"弹窗关闭

- [ ] **Step 4：讨论主界面验证**

检查：
- 左侧 ClockBoard SVG 可见：外圈刻度环、6 段分割、S1–S6 标签在外圈、中心显示 `∞` 或数字
- 右侧聊天：A、B 互发消息，气泡正确显示，我方昵称金色
- 底部条件栏横向排列显示本关所有条件
- TopBar 倒计时正常（讨论计时）
- A（房主）显示"▶ 提前开始出牌"按钮，B 不显示

- [ ] **Step 5：（可选）A 点"提前开始出牌"→ 两端进入 Placing 占位页（M3 stub）**

- [ ] **Step 6：确认无控制台报错后，记录通过**

---

## 自审检查清单（执行前确认）

- [x] `conditionText.ts` 覆盖 levels/README.md 所有 14 种条件类型
- [x] `ClockBoard` 的 `placements` prop 已留接口，M3 无需改签名
- [x] `LevelRulesIntro` 的 `rulesAccepted` 在 Discussion 本地管理，不污染 store
- [x] `LevelSelect` 的 `TOTAL_LEVELS` 常量已标注需随关卡增加更新
- [x] 所有可交互元素：有 `<label>` 或 `aria-label`；`<button>` 而非 `<div onClick>`
- [x] 聊天输入 `maxLength=200`，空内容禁用发送按钮
- [x] `disabled` 按钮不拦截 `onClick`（用 `disabled` 属性而非 `opacity` 糊弄）
- [x] `discussion__conditions` 通过 CSS 覆写 `.condition-list` 实现横向排列，不改组件本身
