# 第 1 关 · 拾级而上

- **关卡编号 (levelIndex)**: 1
- **难度**: ★
- **区段数**: 6（固定）
- **时钟中心值 (centerCap)**: ∞（`"inf"`）—— 本关显式关闭每区段上限，只保留非递减与每段非空两条通用规则
- **牌堆 (deck)**: 全局牌库 24 张（白 1–12 + 黑 1–12），每局随机抽 12 张发牌（本关条件与具体数值无关）
- **设计意图**: 入门关，引入**跨区段关系**与**每段都要放牌**。玩家在看不到队友手牌、桌面又全暗的情况下，要靠讨论约定与提示标记，让 6 个区段的总和从左到右不下降。

## 通关条件

人类可读：

- 各区段总和**非递减**：区段 1 的总和 ≤ 区段 2 ≤ 区段 3 ≤ 区段 4 ≤ 区段 5 ≤ 区段 6。
- 每个区段至少 1 张牌。
- 本关中心值为 ∞，不检查每区段总和上限。

结构化（区段编号 1–6）：

```json
[]
```

> 上述通关条件由加载器自动叠加的永久通用规则提供；本关没有额外特殊条件。

## 备注

- 允许相等（用“非递减 ≤”，不是严格递增）。如需严格递增请改用 `increasing`。
- `centerCap: "inf"` 用于前三关（教学阶段）；第 4 关起 `centerCap: null`，默认中心值为 24。
