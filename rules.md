# Take Time 基本规则总结

> 本文是基于 Libellud《Take Time》公开介绍整理、并结合本项目双人 Web 原型约定改写的中文规则摘要，用于本项目开发参考。它不是官方规则书的逐字翻译，也不包含官方完整关卡、美术或素材。

## 游戏定位

《Take Time》是一款合作式卡牌解谜游戏。玩家需要一起面对一系列“时钟”挑战，通过讨论、记忆、推理和有限沟通，把手牌暗置到时钟的不同区段中，最后揭示并检查是否满足当前时钟的条件。

游戏强调团队协作和有限沟通。玩家一开始可以共同观察挑战并讨论策略，但在看到自己的手牌之后，就不能继续自由沟通，只能依靠之前达成的默契、以及有限的“提示标记”来完成出牌。

## 玩家人数与目标

官方游戏支持 2 到 4 名玩家。本项目初版只实现 2 人模式。

两名玩家作为同一队伍共同挑战时钟。每一关的目标是把全部手牌放到时钟区段上，并在揭示后满足该关的所有条件。如果满足条件，队伍通关；如果不满足，则挑战失败，可以复盘并重试。

## 基本组件抽象

本项目开发时可以把游戏抽象成以下元素：

- 时钟：一关挑战的主面板，**固定包含 6 个区段**，**中心显示一个「中心值」**（见下「时钟中心值」）。
- 区段：玩家放置卡牌的位置，揭示后会按区段统计牌值。
- 卡牌：带有**数值**和**颜色（黑/白）**的手牌，玩家需要暗置到区段中。每名玩家有部分手牌一开始对自己也是暗的（盲牌）。牌面暗置时，数值与颜色一并被遮蔽。
- 提示标记：全队共用的有限资源，出牌时可消耗一个，把该张牌永久正面朝上摆在桌上，用来示意队友。
- 挑战规则：当前时钟的特殊条件，例如总和、顺序、范围、奇偶或相邻关系。
- 队伍状态：当前阶段、当前关卡、两名玩家、手牌、暗置牌、提示标记、揭示结果和通关状态。

## 一局挑战流程

### 1. 观察与讨论

玩家共同查看当前时钟挑战。

在这个阶段，玩家可以自由讨论策略，例如：

- 哪些区段可能需要较大或较小的数值。
- 是否需要保证某些区段递增、递减或相等。
- 如果不能沟通，双方应该如何根据手牌大小做出判断。
- 哪些区段优先放牌，哪些区段留到后面补救。
- 打算把有限的提示标记留给哪些关键位置。

重要限制：玩家此时还不能查看自己的手牌。

本项目允许玩家在开始前设置讨论时间。讨论时间默认是 5 分钟，可选项为 5、10、15、20 分钟。讨论阶段需要在界面上显示倒计时。

玩家可以选择提前结束讨论并直接开始游戏。如果不提前开始，讨论倒计时结束后，系统自动进入发牌与禁沟通阶段。

### 2. 发牌与禁沟通

进入发牌阶段后，每名玩家获得自己的手牌。

本项目双人版约定：

- **牌库为固定的 24 张**：白色 1–12 共 12 张、黑色 1–12 共 12 张（数值与颜色两个维度）。
- **每局开始时系统从这 24 张中随机挑选 12 张**，再按发牌规则下发；因此每局的具体牌面与配色都不同。
- **发牌前先做可解性校验**：系统在抽到 12 张后，会先验证「在当前牌面与归属下，至少存在一种满足本关全部条件的放置方案」；若该牌面无解，则**重新洗牌重抽**，直到抽到可解的一副为止（见下「开发实现要点」）。因此玩家永远不会拿到一副“无论怎么放都不可能通关”的牌。
- 这一局使用挑出的 12 张牌，每名玩家获得 6 张。
- 玩家只能看到自己的手牌，看不到队友的手牌。
- **每名玩家只能看到自己手中中间的 4 张，两端的 2 张对自己也是暗的（盲牌）。**
- 看到手牌后，聊天和自由沟通被禁用。

这个限制是游戏体验的核心。玩家必须依赖讨论阶段制定的策略，而不能在看牌后直接告诉队友自己有什么牌或准备怎么放。

本项目允许玩家在开始前设置两项参数：

- 每回合思考时间：默认 5 秒，可选 5、6、7、8、9、10 秒。
- 提示标记数量：默认 3 个，可选 2、3、4 个，**全队共用**。

进入看牌和出牌阶段后，界面需要显示当前的出牌倒计时。

### 3. 轮流暗置卡牌

玩家按回合出牌。每次出牌时，当前玩家选择自己手中的 1 张牌（可以是自己看不到的盲牌），面朝下放到任意一个时钟区段。

本项目初版约定：

- **第一手为“抢出牌”**：进入出牌阶段后没有预设先手，两名玩家都可以在思考倒计时内出牌；谁先成功出牌，谁就占据第一手，另一方该手被锁。
- 第一手之后**严格交替**：先手方 → 另一方 → 交替，直到 12 张全部放完，自然每人各出 6 张。
- 第一手与之后的每一手，都使用相同的每回合思考时间（默认 5 秒）。
- 每回合只能放 1 张牌；每个区段可以放多张牌。
- **盲牌也可以打出**；打出后在桌上同样面朝下，不显示牌值。
- **当两名玩家都各自累计打出 2 张牌后**，双方手中剩余的盲牌会直接翻开，所有玩家都能看到。
- 已放置的牌保持暗置；玩家不能移动、撤回、交换或查看已经放下的牌。
- **桌上所有打出的牌对双方都是暗的**，唯一例外是使用了提示标记的牌（见下）。
- 当前玩家必须在思考倒计时内完成出牌。如果倒计时结束前没有出牌（抢出牌期双方都没出 / 交替期当前玩家没出），队伍直接判负，进入失败结果阶段。
- 所有 12 张牌全部放完后，自动进入揭示阶段。

#### 提示标记

- 提示标记全队共用，开局可设 2、3、4 个（默认 3），先用先得。
- **每打出一张牌后**，给该玩家 5 秒，用一个 yes/no 弹框决定这张牌是否翻开示意队友。
  - 选 yes 且全队还有剩余标记：消耗 1 个标记，该牌**永久正面朝上**摆在桌上，双方都能看到。
  - 选 no、5 秒超时或标记已用完：不翻开，**不判负**，正常继续。
- 提示窗口期间游戏暂停，玩家做出选择（或超时）后才把回合交给对方。
- 提示标记是无沟通阶段唯一的信息传递手段。

### 4. 揭示与统计

所有牌放完后，系统翻开时钟上的全部卡牌（用提示标记翻开的牌已经是正面）。

结算时需要：

- 显示每个区段放置了哪些牌。
- 计算 6 个区段各自的牌值总和。
- 检查 6 个区段的总和是否从区段 1 到区段 6 **非递减**。
- 检查每个区段是否至少有 1 张牌。
- **若时钟中心值不是 ∞，检查每个区段总和都不超过中心值**（任一区段超出即失败；未显式设置时默认中心值为 24）。
- 检查当前挑战的特殊条件是否全部满足。

如果任一必要条件不满足，则挑战失败。

### 5. 成功、失败与重试

如果所有条件都满足，队伍通过当前时钟挑战，可以进入下一关。

如果失败，队伍可以看到揭示结果并复盘：

- 哪些区段数值不符合预期。
- 哪些策略在禁沟通阶段失效。
- 提示标记是否用在了关键位置。
- 下一次是否需要调整讨论阶段的约定。

失败后可以重试当前关。

## 时钟中心值（每区段上限）

时钟中心显示一个**中心值**，决定**每个区段总和的上限**：

- **中心值为「无限（∞）」**：每个区段的总和**没有上限**，放多大都行（早期教学关常用此设定）。
- **中心值为某个数字（例如 24）**：**每个区段的总和都不得超过该数字**；揭示时只要**任一区段**总和超过中心值，挑战**失败**。
- 关卡数据中 `centerCap` 省略或为 `null` 时，按官方默认中心值 **24** 处理；只有显式写成 `"inf"` 时才表示无限。

中心值是**时钟（关卡）级属性**，对全部 6 个区段统一生效，与各区段自身的条件（总和/范围/奇偶等）叠加判定（同时满足才通关）。它在揭示阶段统一校验，**出牌过程中不做即时拦截**（牌面暗置，过程中拦截会泄露信息且违背盲打机制）。

## 永久通用规则

每一关都会自动叠加以下三条永久通用规则，关卡自己的特殊条件在此基础上继续追加：

- 6 个区段的总和必须从区段 1 到区段 6 **非递减**。
- 每个区段都必须至少有 1 张牌。
- 每个区段总和都不得超过时钟中心值；`centerCap: null` 或省略时默认是 24，`centerCap: "inf"` 时不检查上限。

## 常见挑战条件示例

为了避免复刻官方 40 个关卡，本项目初版应使用原创挑战条件。下面是适合原型开发的条件类型（都在固定 6 个区段上）：

- 总和目标：某个区段的总和必须等于指定数字。
- 范围目标：某个区段的总和必须在指定范围内。
- 递增目标：若干区段的总和必须从小到大排列。
- 递减目标：若干区段的总和必须从大到小排列。
- 奇偶目标：某些区段必须为奇数或偶数。
- 差值目标：相邻两个区段的总和差值必须满足限制。
- 空间目标：所有区段都必须至少有 1 张牌（永久通用规则，通常无需在关卡特殊条件中重复书写）。
- **每区段上限目标（时钟中心值）**：每个区段的总和都不超过中心值（永久通用规则；中心值为 ∞ 时即无此约束）。
- 组合目标：同时满足多个简单条件。

初版关卡应从简单条件开始，再逐步增加组合条件，方便测试玩法闭环。

## 本项目双人 Web 版约定

本项目不是完整官方复刻，而是一个私用 Web 原型。初版采用以下简化规则：

- 只支持 2 名玩家。
- 只有一个全局房间。
- 时钟固定 6 个区段；每关都必须满足非递减、每段非空、每段不超过中心值三条通用规则，再叠加本关特殊条件。
- 时钟中心值决定每区段总和上限（省略/`null` = 默认 24，`"inf"` = 无上限，数字 = 每段不得超过该值）。
- 牌库固定 24 张（白 1–12、黑 1–12）；每局随机抽 12 张，每名玩家固定 6 张手牌。
- 每名玩家只能看到自己手中中间的 4 张，两端 2 张为盲牌；当两名玩家都各自打出 2 张后，双方剩余盲牌直接公开给所有玩家。
- 出牌为“抢先手 + 之后交替”：第一手两人可抢，先出者占手，之后严格交替。
- 桌上所有牌默认暗置，只有用提示标记翻开的牌永久正面朝上。
- 提示标记全队共用，默认 3 个，可选 2、3、4 个；每次出牌后有 5 秒 yes/no 决定是否使用。
- 讨论阶段允许聊天。
- 讨论时间默认 5 分钟，可选 5、10、15、20 分钟。
- 讨论阶段显示倒计时，玩家可以提前结束讨论并开始游戏。
- 如果玩家不提前开始，讨论倒计时结束后自动进入发牌和禁沟通阶段。
- 看牌后禁用聊天。
- 每回合思考时间默认 5 秒，可选 5、6、7、8、9、10 秒。
- 出牌阶段显示当前玩家倒计时；当前玩家超时未出牌时，队伍直接判负（提示窗口超时不判负）。
- 所有牌放完后自动统一揭示。
- 使用原创时钟关卡和占位视觉。
- 不使用官方美术、官方卡牌图、官方完整规则书文本或官方 40 个关卡内容。

其中，计时/超时判负、提示标记数量配置、以及每次出牌后的 yes/no 提示窗口，是本项目为了实时双人 Web 体验做的数字化扩展，并非官方桌游原规则的逐字复刻。

> 待补全项：6 个区段各关的具体条件设计，由后续设计确定。（牌库已定为 24 张：白 1–12、黑 1–12，每局随机抽 12 张。）

## 开发实现要点

服务端应作为权威状态来源，负责校验所有关键操作：

- 当前是否已经有两名玩家。
- 当前是否处于允许操作的阶段。
- 抢出牌期的并发仲裁：第一个有效出牌生效，其余拒绝。
- 交替期是否轮到该玩家出牌。
- 玩家是否拥有要出的那张牌（盲牌允许打出）。
- 目标区段是否合法（区段索引 0–5）。
- 当前玩家是否在思考倒计时内完成出牌。
- 提示决策是否由该出牌玩家、在 5 秒窗口内做出，且全队标记未超额。
- 两名玩家都各自打满 2 张后，翻开双方剩余盲牌并公开给所有玩家。
- 是否已经放满 12 张牌。
- 揭示时是否满足永久通用规则和当前关卡特殊条件。

发牌时还需保证**可解性**：抽出 12 张候选牌后，先用求解器（`server/src/game/solver.ts` 的 `canSolveDeal`）判定该牌面在本关条件下是否至少存在一种合法放置方案（含出牌顺序类条件的可行性）；无解则重抽，直至可解或达到重试上限。求解器对放置类条件做强剪枝回溯，并对 `placement-order` 单独校验是否存在合法出牌顺序。可用 `npm run assess:levels` 评估各关在随机牌面下的可解率。

前端只负责展示状态和发送玩家意图，不能自行决定胜负。手牌可见性、桌面暗牌牌值都由服务端按规则遮蔽后再下发。

## 参考资料

- Libellud 官方 Take Time 页面：https://www.libellud.com/en/our-games/take-time/
